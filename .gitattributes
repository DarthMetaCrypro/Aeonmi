## Enforce consistent line endings and prepare for potential LFS
* text=auto eol=lf

# Binary (never auto-normalize)
*.png binary
*.ico binary
*.exe binary
*.pdb binary
*.dll binary
*.wasm binary

# If adopting Git LFS, uncomment lines below and install git-lfs, then run:
#   git lfs install
#   git lfs track "*.pdb"
#   git add .gitattributes
#
# *.pdb filter=lfs diff=lfs merge=lfs -text
# *.rmeta filter=lfs diff=lfs merge=lfs -text
